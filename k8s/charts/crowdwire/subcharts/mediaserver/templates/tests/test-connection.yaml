apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mediaserver.fullname" . }}-test-connection"
  labels:
    {{- include "mediaserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mediaserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
